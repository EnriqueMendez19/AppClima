import { ValidateAccesDirective } from './validate-acces.directive';

describe('ValidateAccesDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateAccesDirective();
    expect(directive).toBeTruthy();
  });
});
