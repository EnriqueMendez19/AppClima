import { CapitalleterPipe } from './capitalleter.pipe';

describe('CapitalleterPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalleterPipe();
    expect(pipe).toBeTruthy();
  });
});
